import React, { useState } from 'react';

const CreateLink = () => {
    const [formState, setFormState] = use
}

export default function Create() {
    return (
        <div>

        </div>
    );
}